# Upstream ML package is installed separately with --no-deps after these dependencies.
# This prevents its unpinned Git dependency from replacing the reviewed SDK.
label-studio-sdk==2.1.1
Flask==3.1.3
colorama==0.4.6
requests==2.34.2
semver==3.0.4
waitress==3.0.2
httpx==0.28.1
